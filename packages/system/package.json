{
  "name": "@effect-ts/system",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "repository": "https://github.com/Effect-TS/core.git",
  "homepage": "https://www.matechs.com",
  "scripts": {
    "clean": "yarn ets:rimraf build",
    "build-ts": "yarn ets:tsc -b ./tsconfig.build.json",
    "build-out": "sh build-out.sh",
    "build": "yarn build-ts && yarn build-out && yarn circular",
    "lint": "yarn ets:eslint . --ext .ts,.tsx",
    "autofix": "yarn prettier && yarn lint --fix && yarn prettier",
    "prettier": "yarn ets:prettier --write \"./{src,test,demo}/**/*.ts\"",
    "tc": "yarn ets:tsc --noEmit",
    "tc:watch": "yarn ets:tsc --noEmit --watch",
    "demo": "yarn ets:ts-node -O '{\"module\":\"commonjs\"}' --project tsconfig.build.json",
    "pack-dev": "yarn ets:rimraf artifacts && mkdir artifacts && yarn pack --out ./artifacts/%s-%v.tgz",
    "circular": "yarn ets:madge --circular --no-color --no-spinner --warning build/out"
  },
  "publishConfig": {
    "access": "public"
  },
  "typesVersions": {
    "*": {
      "*": [
        "./build/dts/*"
      ]
    }
  },
  "exports": {
    "./*": {
      "require": "./build/cjs/*.cjs",
      "import": "./build/mjs/*.mjs"
    }
  },
  "tsplus": {
    "typeDir": "./build/dts"
  }
}
