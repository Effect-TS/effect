import { size as size_1 } from "../Readonly/Record"

/**
 * Calculate the number of key/value pairs in a record
 *
 * @since 2.0.0
 */
export const size: (r: Record<string, unknown>) => number = size_1
