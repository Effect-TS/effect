import type { Partition1 } from "fp-ts/lib/Filterable"

import { partition_ as partition__1 } from "../Readonly/Array/partition_"

import { URI } from "./URI"

export const partition_: Partition1<URI> = partition__1 as any
