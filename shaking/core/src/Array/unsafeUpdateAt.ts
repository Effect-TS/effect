import { unsafeUpdateAt as unsafeUpdateAt_1 } from "../Readonly/Array/unsafeUpdateAt"

/**
 * @since 2.0.0
 */
export const unsafeUpdateAt: <A>(
  i: number,
  a: A,
  as: Array<A>
) => Array<A> = unsafeUpdateAt_1 as any
