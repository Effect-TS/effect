import { isOutOfBound as isOutOfBound_1 } from "../Readonly/Array/isOutOfBound"

/**
 * Test whether an array contains a particular index
 *
 * @since 2.0.0
 */
export const isOutOfBound: <A>(i: number, as: Array<A>) => boolean = isOutOfBound_1
