---
"effect": patch
---

Persist synchronous lookup defects in `PersistedCache` so subsequent cache instances replay them like defects returned by lookup effects, using the configured persistent TTL.
