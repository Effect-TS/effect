---
"effect": patch
---

Add `Schema.resolveAnnotationsKey` API to retrieve the context (key-level) annotations from a schema, closes #1947.

Also rename `Schema.resolveInto` to `Schema.resolveAnnotations`.
