---
"effect": patch
---

Fix `Metric` series selection for colliding ids, descriptions and attributes, empty attributes, and updates after a registry is cleared. Cache series keys for attributed metrics until the contextual attributes change.
