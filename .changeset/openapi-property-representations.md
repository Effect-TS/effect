---
"effect": patch
---

Generate OpenAPI parameters and response headers from their encoded object representation. Property annotations are now preserved, and an absent optional HTTP value is no longer emitted as a nullable JSON value.
