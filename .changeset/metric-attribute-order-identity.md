---
"effect": patch
---

Fix metrics with the same attributes in different orders to share one series, while keeping distinct attribute values separate and preserving caller and public metadata order.
