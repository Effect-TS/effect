---
"@effect-native/libcrsql": none
---

feat(libcrsql): add @effect-native/libcrsql providing absolute paths to cr-sqlite v0.16.3 binaries for macOS (arm64/x64), Linux (arm64/x64), and Windows (x64/i686), plus an optional Effect entrypoint with TaggedError handling. Includes exports for root, paths, and effect entrypoints; browser guard; tests; docgen config; and a maintainer script to fetch binaries.

Note: Version is pinned to 0.16.3 to match upstream. Release will proceed after manual platform verification.

