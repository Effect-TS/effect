---
"effect": patch
---

Generate BigDecimal arbitraries across ordinary, very small and very large magnitudes even at small sizes. Vary scales under range constraints instead of collapsing one-sided bounds to a small set of tiny fractions, and construct inclusive and exclusive decimal bounds without rejection sampling.
