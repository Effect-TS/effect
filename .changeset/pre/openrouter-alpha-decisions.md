---
"@effect/ai-openrouter": patch
---

Add `OpenRouterDecisionModel` for OpenRouter's alpha Decisions API.

Custom `OpenRouterClient.Service` implementations and mocks must now provide `createDecisions`.
