---
"effect": patch
---

Use the negotiated stateful fallback for MCP stdio batch handling when a server also supports stateless protocols. Clients can send batches when the fallback protocol supports them.
