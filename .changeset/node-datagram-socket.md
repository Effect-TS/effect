---
"@effect/platform-node-shared": minor
"@effect/platform-node": minor
---

Add `NodeDatagramSocket`, a `node:dgram` adapter for `effect/socket/DatagramSocket` with `make`, `fromSocket` and `layer`, re-exported from `@effect/platform-node`. `@effect/platform-node-shared` also gets a `benchmark:datagram` script that compares it with raw `node:dgram`.
