---
"effect": patch
---

Add `Decision` and `DecisionModel` to `effect/unstable/ai` for classification, rating, and probability estimates. Batch named decisions over one schema-encoded input and receive typed answers with token usage. Classification and rating answers include full probability distributions and optional provider-reported confidence.
