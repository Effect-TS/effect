---
"effect": patch
---

Release non-persisted, interruptible `RunnerServer` handlers and mailbox slots when callers disconnect.

Preserve dynamic `WithTransaction` annotations during replay and re-delivery.
