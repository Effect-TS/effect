---
"@effect/ai-google-vertex": minor
---

Add `@effect/ai-google-vertex`, a Google Vertex AI (Gemini) provider for the Effect AI SDK. It implements the `LanguageModel` and `EmbeddingModel` services on top of the Vertex AI REST API, with OAuth / API key authentication, tool calling, structured outputs, streaming, and grounding tools.
