---
"effect": minor
---

Add opt-in `SchemaBinary` fingerprint mode. It uses positional layouts and an 8-byte layout hash for smaller frames, rejecting peers compiled from a different schema definition.
