"effect/ScheduleDecision#continue":
  replacement: "Effect.succeed([output, duration])"
  note: "ScheduleDecision was removed from the v4 public model. A Schedule.fromStep step recurs by returning [output, Duration] and terminates with Cause.done(output)."
"effect/ScheduleDecision#continueWith":
  replacement: "Effect.succeed([output, duration])"
  note: "ScheduleDecision was removed from the v4 public model. A Schedule.fromStep step recurs by returning [output, Duration] and terminates with Cause.done(output)."
"effect/ScheduleDecision#Done":
  replacement: "Cause.Done"
  note: "ScheduleDecision was removed from the v4 public model. A Schedule.fromStep step recurs by returning [output, Duration] and terminates with Cause.done(output)."
"effect/ScheduleDecision#isContinue":
  replacement: "none"
  note: "ScheduleDecision was removed from the v4 public model. A Schedule.fromStep step recurs by returning [output, Duration] and terminates with Cause.done(output). Branch on the Pull result instead of inspecting a decision value."
"effect/ScheduleDecision#isDone":
  replacement: "Cause.isDone"
  note: "ScheduleDecision was removed from the v4 public model. A Schedule.fromStep step recurs by returning [output, Duration] and terminates with Cause.done(output)."
"effect/ScheduleDecision#ScheduleDecision":
  replacement: "none"
  note: "ScheduleDecision was removed from the v4 public model. A Schedule.fromStep step recurs by returning [output, Duration] and terminates with Cause.done(output)."
