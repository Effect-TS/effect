"effect/TestConfig#make":
  replacement: "{ repeats, retries, samples, shrinks }"
  note: "The v3 constructor only returned its parameter object. The TestConfig service was removed; keep a plain object only for application-owned configuration."
"effect/TestConfig#TestConfig":
  replacement: "none"
  note: "There is no v4 TestConfig service. Move runner settings to Vitest and FastCheck options, or define an application-specific Context.Reference if runtime access is needed."
