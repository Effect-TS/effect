"@effect/cluster":
  replacement: "effect/cluster"
  note: "The @effect/cluster package was merged into the effect package; import the effect/cluster barrel or import specific modules directly (e.g. effect/cluster/<Module>)."
