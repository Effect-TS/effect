effect/Effect#acquireReleaseInterruptible:
  replacement: "Effect.acquireRelease"
  note: "Pass `{ interruptible: true }` in the options object. Adapt arguments and imports to the v4 API."
effect/Effect#Adapter:
  replacement: "none"
  note: "The generator adapter type was removed; yield Effect values directly inside `Effect.gen`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#All:
  replacement: "Effect.All"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#All.ExtractMode:
  replacement: "Effect.All.Return"
  note: "The `either` extraction helper was removed; use `mode: \"result\"` and the v4 return helper. Adapt arguments and imports to the v4 API."
effect/Effect#allowInterrupt:
  replacement: "Effect.yieldNow"
  note: "Yield to the scheduler to create an interruptible checkpoint. Adapt arguments and imports to the v4 API."
effect/Effect#allSuccesses:
  replacement: "Effect.all"
  note: "Run with `{ mode: \"result\" }`, then retain `Result.Success` values. Adapt arguments and imports to the v4 API."
effect/Effect#allWith:
  replacement: "Effect.all"
  note: "Wrap `Effect.all(values, options)` in a lambda when a data-last combinator is needed. Adapt arguments and imports to the v4 API."
effect/Effect#annotateLogs:
  replacement: "Effect.annotateLogs"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#ap:
  replacement: "Effect.zipWith"
  note: "Zip the function effect and value effect, then apply the function in the combiner. Adapt arguments and imports to the v4 API."
effect/Effect#asSomeError:
  replacement: "Effect.mapError"
  note: "Map errors with `Option.some`. Adapt arguments and imports to the v4 API."
effect/Effect#async:
  replacement: "Effect.callback"
  note: "Use the renamed callback constructor. Adapt arguments and imports to the v4 API."
effect/Effect#asyncEffect:
  replacement: "Effect.callback"
  note: "The callback registration may return an Effect cleanup action in v4. Adapt arguments and imports to the v4 API."
effect/Effect#bindAll:
  replacement: "Effect.bind + Effect.all"
  note: "Bind the result of `Effect.all` explicitly in the do-notation pipeline. Adapt arguments and imports to the v4 API."
effect/Effect#blocked:
  replacement: "none"
  note: "The request-runtime blocked constructor is internal; express work with `Effect.request` and a `RequestResolver`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#Blocked:
  replacement: "none"
  note: "The request-runtime blocked model is internal; use public `Request` and `RequestResolver` APIs. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#cachedFunction:
  replacement: "none"
  note: "The function memoizer was removed; use `Cache` for keyed effectful caching. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#cacheRequestResult:
  replacement: "none"
  note: "Direct request-cache mutation was removed; configure request resolution through `RequestResolver`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#catch:
  replacement: "Effect.catch"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#catchAll:
  replacement: "Effect.catch"
  note: "Use the shortened v4 error-handler name. Adapt arguments and imports to the v4 API."
effect/Effect#catchAllCause:
  replacement: "Effect.catchCause"
  note: "Use the shortened v4 cause-handler name. Adapt arguments and imports to the v4 API."
effect/Effect#catchAllDefect:
  replacement: "Effect.catchDefect"
  note: "Use the shortened v4 defect-handler name. Adapt arguments and imports to the v4 API."
effect/Effect#catchSome:
  replacement: "Effect.catchFilter"
  note: "Replace the Option-returning partial function with a `Filter` and handler. Adapt arguments and imports to the v4 API."
effect/Effect#catchSomeCause:
  replacement: "Effect.catchCauseFilter"
  note: "Replace the Option-returning partial function with a cause `Filter` and handler. Adapt arguments and imports to the v4 API."
effect/Effect#catchSomeDefect:
  replacement: "none"
  note: "Use `Effect.catchDefect` and branch explicitly, re-dying for unmatched defects. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#cause:
  replacement: "Effect.exit"
  note: "Inspect `Exit.Failure.cause`; v4 no longer exposes an Effect-only cause extractor. Adapt arguments and imports to the v4 API."
effect/Effect#checkInterruptible:
  replacement: "none"
  note: "Interruptibility introspection was removed; structure the region explicitly with `Effect.interruptible` or `Effect.uninterruptible`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#clock:
  replacement: "Clock.Clock"
  note: "Services are Effects in v4; yield or compose `Clock.Clock` directly. Adapt imports to the v4 API."
effect/Effect#configProviderWith:
  replacement: "ConfigProvider.ConfigProvider.use"
  note: "Use the ConfigProvider reference's `.use` helper to invoke the effectful callback. Adapt arguments and imports to the v4 API."
effect/Effect#console:
  replacement: "Console.Console"
  note: "Services are Effects in v4; yield or compose `Console.Console` directly. Adapt imports to the v4 API."
effect/Effect#consoleWith:
  replacement: "Console.Console.use"
  note: "Use the Console reference's `.use` helper to invoke the effectful callback. Adapt arguments and imports to the v4 API."
effect/Effect#contextWithEffect:
  replacement: "Effect.contextWith"
  note: "`contextWith` accepts an effectful callback in v4. Adapt arguments and imports to the v4 API."
effect/Effect#currentPropagatedSpan:
  replacement: "Effect.currentParentSpan"
  note: "Use the current parent span representation. Adapt arguments and imports to the v4 API."
effect/Effect#custom:
  replacement: "none"
  note: "The low-level custom instruction constructor was removed; use public constructors such as `Effect.sync`, `Effect.suspend`, or `Effect.callback`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#daemonChildren:
  replacement: "Effect.awaitAllChildren"
  note: "Use structured child-fiber waiting, or fork explicitly with `Effect.forkDetach` when detachment is intended. Adapt arguments and imports to the v4 API."
effect/Effect#descriptor:
  replacement: "Effect.fiberId"
  note: "The full fiber descriptor was removed; retrieve the current numeric fiber id. Adapt arguments and imports to the v4 API."
effect/Effect#descriptorWith:
  replacement: "Effect.fiberId + Effect.flatMap"
  note: "Read the current fiber id and invoke the callback explicitly. Adapt arguments and imports to the v4 API."
effect/Effect#dieMessage:
  replacement: "Effect.die"
  note: "Construct the desired defect explicitly and pass it to `Effect.die`. Adapt arguments and imports to the v4 API."
effect/Effect#dieSync:
  replacement: "Effect.suspend + Effect.die"
  note: "Evaluate the lazy defect inside `Effect.suspend`. Adapt arguments and imports to the v4 API."
effect/Effect#diffFiberRefs:
  replacement: "none"
  note: "The public FiberRefs diff API was removed; model fiber-local state with context references and scoped `Effect.provideService`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#disconnect:
  replacement: "Effect.forkDetach"
  note: "Fork explicitly and decide how to await or interrupt the detached Fiber. Adapt arguments and imports to the v4 API."
effect/Effect#dropUntil:
  replacement: "none"
  note: "Use an explicit `Effect.gen` loop for an effectful stopping predicate. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#dropWhile:
  replacement: "none"
  note: "Use an explicit `Effect.gen` loop, or `Array.dropWhile` when the predicate is pure. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#Effect:
  replacement: "Effect.Effect"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#Effect.AsEffect:
  replacement: "Effect.Effect"
  note: "Use the Effect type directly and extract channels with `Effect.Success`, `Effect.Error`, and `Effect.Services`. Adapt arguments and imports to the v4 API."
effect/Effect#Effect.Context:
  replacement: "Effect.Services"
  note: "Use the renamed type-level extractor for required services. Adapt arguments and imports to the v4 API."
effect/Effect#Effect.VarianceStruct:
  replacement: "Effect.Variance"
  note: "Use the v4 variance interface. Adapt arguments and imports to the v4 API."
effect/Effect#EffectGenerator:
  replacement: "Effect.EffectIterator"
  note: "Use the v4 iterator type used by generator delegation. Adapt arguments and imports to the v4 API."
effect/Effect#EffectTypeId:
  replacement: "Effect.TypeId"
  note: "Use the v4 type-level Effect identifier. Adapt arguments and imports to the v4 API."
effect/Effect#EffectUnify:
  replacement: "Effect.EffectUnify"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#EffectUnifyIgnore:
  replacement: "none"
  note: "The internal unification-ignore helper is no longer public; rely on v4 Effect inference. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#either:
  replacement: "Effect.result"
  note: "V4 represents typed success/failure as `Result` instead of `Either`. Adapt arguments and imports to the v4 API."
effect/Effect#ensureErrorType:
  replacement: "Effect.satisfiesErrorType"
  note: "Use the renamed compile-time channel constraint. Adapt arguments and imports to the v4 API."
effect/Effect#ensureRequirementsType:
  replacement: "Effect.satisfiesServicesType"
  note: "Use the renamed compile-time services constraint. Adapt arguments and imports to the v4 API."
effect/Effect#ensureSuccessType:
  replacement: "Effect.satisfiesSuccessType"
  note: "Use the renamed compile-time channel constraint. Adapt arguments and imports to the v4 API."
effect/Effect#ensuringChild:
  replacement: "Effect.ensuring + Fiber APIs"
  note: "Track the child Fiber explicitly and run the finalizer with `Effect.ensuring`. Adapt arguments and imports to the v4 API."
effect/Effect#ensuringChildren:
  replacement: "Effect.awaitAllChildren + Effect.ensuring"
  note: "Use structured child waiting and an explicit finalizer. Adapt arguments and imports to the v4 API."
effect/Effect#every:
  replacement: "Effect.forEach"
  note: "Evaluate predicates with `Effect.forEach`, then test the resulting booleans with `Array.every`. Adapt arguments and imports to the v4 API."
effect/Effect#exists:
  replacement: "Effect.findFirst"
  note: "Find the first value satisfying the effectful predicate and test the returned Option. Adapt arguments and imports to the v4 API."
effect/Effect#fiberIdWith:
  replacement: "Effect.fiberId + Effect.flatMap"
  note: "Read the numeric fiber id and invoke the callback explicitly. Adapt arguments and imports to the v4 API."
effect/Effect#filterEffectOrElse:
  replacement: "Effect.flatMap"
  note: "Evaluate the effectful predicate and branch to `Effect.succeed` or the fallback. Adapt arguments and imports to the v4 API."
effect/Effect#filterEffectOrFail:
  replacement: "Effect.flatMap"
  note: "Evaluate the effectful predicate and branch to `Effect.succeed` or `Effect.fail`. Adapt arguments and imports to the v4 API."
effect/Effect#filterOrDie:
  replacement: "Effect.filterOrFail + Effect.orDie"
  note: "Filter with a typed failure, then convert it to a defect. Adapt arguments and imports to the v4 API."
effect/Effect#filterOrDieMessage:
  replacement: "Effect.filterOrFail + Effect.orDie"
  note: "Create the message-bearing error in `filterOrFail`, then convert it to a defect. Adapt arguments and imports to the v4 API."
effect/Effect#finalizersMask:
  replacement: "none"
  note: "Configurable finalizer execution strategies were removed; register ordered finalizers explicitly in a Scope. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#flipWith:
  replacement: "Effect.flip"
  note: "Flip, apply the transformation, then flip the resulting Effect back. Adapt arguments and imports to the v4 API."
effect/Effect#fn:
  replacement: "Effect.fn"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#fn.Gen:
  replacement: "Effect.fn.Return"
  note: "Use the v4 generator-return helper type. Adapt arguments and imports to the v4 API."
effect/Effect#fn.NonGen:
  replacement: "Effect.fn.Untraced"
  note: "Use the v4 function helper type for non-generator wrapping. Adapt arguments and imports to the v4 API."
effect/Effect#fork:
  replacement: "Effect.forkChild"
  note: "Use the renamed structured child-fiber combinator. Adapt arguments and imports to the v4 API."
effect/Effect#forkAll:
  replacement: "Effect.forEach + Effect.forkChild"
  note: "Fork each effect explicitly, or prefer a higher-level concurrent combinator. Adapt arguments and imports to the v4 API."
effect/Effect#forkDaemon:
  replacement: "Effect.forkDetach"
  note: "Use the renamed detached-fiber combinator. Adapt arguments and imports to the v4 API."
effect/Effect#forkWithErrorHandler:
  replacement: "Effect.forkChild + Fiber.await"
  note: "Fork explicitly and observe the Fiber result to handle errors. Adapt arguments and imports to the v4 API."
effect/Effect#fromFiber:
  replacement: "Fiber.join"
  note: "Join the Fiber to obtain an Effect of its result. Adapt arguments and imports to the v4 API."
effect/Effect#fromFiberEffect:
  replacement: "Effect.flatMap + Fiber.join"
  note: "FlatMap the effectful Fiber and join it. Adapt arguments and imports to the v4 API."
effect/Effect#fromNullable:
  replacement: "Effect.fromOption + Option.fromNullable"
  note: "Convert the nullable value to Option, then lift it into Effect. Adapt arguments and imports to the v4 API."
effect/Effect#functionWithSpan:
  replacement: "Effect.withSpan"
  note: "Wrap the function body with a span whose name/options are derived from its arguments. Adapt arguments and imports to the v4 API."
effect/Effect#FunctionWithSpanOptions:
  replacement: "Tracer.SpanOptions"
  note: "Use the v4 tracing options type when wrapping functions with `Effect.withSpan`. Adapt arguments and imports to the v4 API."
effect/Effect#gen:
  replacement: "Effect.gen"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#getFiberRefs:
  replacement: "none"
  note: "The FiberRefs collection is no longer public; access individual context references through Effect services. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#getRuntimeFlags:
  replacement: "none"
  note: "RuntimeFlags are no longer a public Effect service; use supported high-level runtime options. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#head:
  replacement: "Effect.flatMap + Array.head + Effect.fromOption"
  note: "Inspect the produced iterable explicitly and fail when it is empty. Adapt arguments and imports to the v4 API."
effect/Effect#if:
  replacement: "Effect.suspend"
  note: "Select the branch lazily with a JavaScript conditional inside `Effect.suspend`. Adapt arguments and imports to the v4 API."
effect/Effect#ignoreLogged:
  replacement: "Effect.ignore"
  note: "Pass `{ log: true }` to the consolidated ignore combinator. Adapt arguments and imports to the v4 API."
effect/Effect#inheritFiberRefs:
  replacement: "none"
  note: "Bulk FiberRef inheritance was removed; propagate required context references explicitly. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#interruptWith:
  replacement: "Effect.interrupt"
  note: "V4 interruption uses the current fiber identity; remove the explicit FiberId argument. Adapt arguments and imports to the v4 API."
effect/Effect#intoDeferred:
  replacement: "Deferred.into"
  note: "Use the Deferred module combinator. Adapt arguments and imports to the v4 API."
effect/Effect#iterate:
  replacement: "none"
  note: "Use an explicit stateful `Effect.gen` loop; v4 removed the Effect-specific loop helper. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#labelMetrics:
  replacement: "Metric.withAttributes"
  note: "Convert labels to metric attributes and scope them around the Effect. Adapt arguments and imports to the v4 API."
effect/Effect#labelMetricsScoped:
  replacement: "Metric.withAttributes"
  note: "Apply metric attributes to the scoped Effect rather than mutating scoped labels. Adapt arguments and imports to the v4 API."
effect/Effect#LatchUnify:
  replacement: "Latch.Latch"
  note: "Latch moved to the standalone `effect/Latch` module; rely on normal v4 inference. Adapt arguments and imports to the v4 API."
effect/Effect#LatchUnifyIgnore:
  replacement: "none"
  note: "The internal Latch unification helper was removed; use `Latch.Latch` directly. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#let:
  replacement: "Effect.let"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#liftPredicate:
  replacement: "Effect.filterOrFail"
  note: "Lift the input with `Effect.succeed` and apply `filterOrFail`. Adapt arguments and imports to the v4 API."
effect/Effect#linkSpanCurrent:
  replacement: "Effect.linkSpans"
  note: "Use the v4 span-link combinator. Adapt arguments and imports to the v4 API."
effect/Effect#locally:
  replacement: "Effect.provideService"
  note: "FiberRef values are context references in v4; provide the reference for the Effect lifetime. Adapt arguments and imports to the v4 API."
effect/Effect#locallyScoped:
  replacement: "Effect.provideService"
  note: "Provide the context reference around the scoped Effect. Adapt arguments and imports to the v4 API."
effect/Effect#locallyScopedWith:
  replacement: "Effect.updateServiceScoped"
  note: "Context references replace FiberRefs in v4; update the reference for the current scope. Adapt arguments and imports to the v4 API."
effect/Effect#locallyWith:
  replacement: "Effect.updateService"
  note: "Context references replace FiberRefs in v4; update the reference around the target Effect. Adapt arguments and imports to the v4 API."
effect/Effect#logAnnotations:
  replacement: "References.CurrentLogAnnotations"
  note: "Context references are Effects in v4; yield or compose `References.CurrentLogAnnotations` directly. Adapt imports to the v4 API."
effect/Effect#loop:
  replacement: "none"
  note: "Use an explicit `Effect.gen` loop and collect results when needed. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#makeLatch:
  replacement: "Latch.make"
  note: "Latch constructors moved to `effect/Latch`. Adapt arguments and imports to the v4 API."
effect/Effect#makeSemaphore:
  replacement: "Semaphore.make"
  note: "Semaphore constructors moved to `effect/Semaphore`. Adapt arguments and imports to the v4 API."
effect/Effect#mapAccum:
  replacement: "Effect.reduce"
  note: "Carry `[state, output]` through an effectful reduction. Adapt arguments and imports to the v4 API."
effect/Effect#mapErrorCause:
  replacement: "Effect.catchCause + Effect.failCause"
  note: "Transform the Cause in a cause handler and fail with the mapped Cause. Adapt arguments and imports to the v4 API."
effect/Effect#mapInputContext:
  replacement: "Effect.contextWith + Effect.provide"
  note: "Build the required context from the incoming context and provide it explicitly. Adapt arguments and imports to the v4 API."
effect/Effect#merge:
  replacement: "Effect.catch"
  note: "Recover each typed error with `Effect.succeed` so both channels become success values. Adapt arguments and imports to the v4 API."
effect/Effect#mergeAll:
  replacement: "Effect.reduce"
  note: "Reduce the input effects with an effectful accumulator. Adapt arguments and imports to the v4 API."
effect/Effect#metricLabels:
  replacement: "Metric.CurrentMetricAttributes"
  note: "Context references are Effects in v4; yield or compose `Metric.CurrentMetricAttributes` directly. Adapt imports to the v4 API."
effect/Effect#negate:
  replacement: "Effect.map"
  note: "Map the boolean result with logical negation. Adapt arguments and imports to the v4 API."
effect/Effect#none:
  replacement: "Effect.flatMap + Option.match"
  note: "Fail for `Some` and succeed with void for `None`. Adapt arguments and imports to the v4 API."
effect/Effect#once:
  replacement: "Effect.cached"
  note: "Create the cached Effect once, then execute the returned Effect repeatedly. Adapt arguments and imports to the v4 API."
effect/Effect#optionFromOptional:
  replacement: "Effect.catchTag"
  note: "Map success to `Option.some` and recover `NoSuchElementError` with `Option.none`. Adapt arguments and imports to the v4 API."
effect/Effect#orDieWith:
  replacement: "Effect.mapError + Effect.orDie"
  note: "Map the typed error to the desired defect, then convert failures to defects. Adapt arguments and imports to the v4 API."
effect/Effect#orElse:
  replacement: "Effect.catch"
  note: "Ignore the caught error and evaluate the fallback Effect. Adapt arguments and imports to the v4 API."
effect/Effect#orElseFail:
  replacement: "Effect.mapError"
  note: "Replace every typed error with the lazily produced failure value. Adapt arguments and imports to the v4 API."
effect/Effect#parallelErrors:
  replacement: "Effect.all"
  note: "Use `{ mode: \"result\", concurrency: \"unbounded\" }` and collect failures explicitly. Adapt arguments and imports to the v4 API."
effect/Effect#parallelFinalizers:
  replacement: "none"
  note: "Parallel finalizer strategy mutation was removed; fork independent cleanup explicitly when ordering is irrelevant. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#patchFiberRefs:
  replacement: "none"
  note: "Bulk FiberRefs patching was removed; update individual context references with `Effect.updateService`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#patchRuntimeFlags:
  replacement: "none"
  note: "RuntimeFlags patching was removed from the public API; use supported high-level runtime options. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#Permit:
  replacement: "Semaphore.Semaphore"
  note: "Use the standalone Semaphore API and its `withPermit` / `withPermits` methods. Adapt arguments and imports to the v4 API."
effect/Effect#raceWith:
  replacement: "Effect.raceFirst + Fiber APIs"
  note: "Use `raceFirst` for first completion, or fork both effects and inspect their Exits for custom finishers. Adapt arguments and imports to the v4 API."
effect/Effect#random:
  replacement: "Random.Random"
  note: "Services are Effects in v4; yield or compose `Random.Random` directly. Adapt imports to the v4 API."
effect/Effect#randomWith:
  replacement: "Random.Random.use"
  note: "Use the Random reference's `.use` helper to invoke the effectful callback. Prefer module-level Random operations when possible."
effect/Effect#reduceEffect:
  replacement: "Effect.flatMap + Effect.reduce"
  note: "Evaluate the initial Effect, then reduce the remaining effects. Adapt arguments and imports to the v4 API."
effect/Effect#reduceRight:
  replacement: "Effect.reduce"
  note: "Reverse the input first, then perform the effectful reduction. Adapt arguments and imports to the v4 API."
effect/Effect#reduceWhile:
  replacement: "none"
  note: "Use an explicit `Effect.gen` loop that checks the accumulator before each step. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#Repeat:
  replacement: "Effect.Repeat"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#Repeat.Options:
  replacement: "Effect.Repeat.Options"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#repeatN:
  replacement: "Effect.repeat"
  note: "Pass `{ times: n }` to the consolidated repeat combinator. Adapt arguments and imports to the v4 API."
effect/Effect#Retry:
  replacement: "Effect.Retry"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#Retry.Options:
  replacement: "Effect.Retry.Options"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#runRequestBlock:
  replacement: "none"
  note: "The request-runtime block runner is internal; submit requests with `Effect.request`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#runtime:
  replacement: "Effect.context + Effect.runForkWith"
  note: "Capture services as a Context and use the corresponding `run*With` function. Adapt arguments and imports to the v4 API."
effect/Effect#scheduleForked:
  replacement: "Effect.schedule + Effect.forkScoped"
  note: "Schedule the Effect, then fork it in the current Scope. Adapt arguments and imports to the v4 API."
effect/Effect#scopeWith:
  replacement: "Effect.scopedWith"
  note: "Use the renamed scoped callback combinator. Adapt arguments and imports to the v4 API."
effect/Effect#sequentialFinalizers:
  replacement: "none"
  note: "Sequential reverse-order finalization is the normal Scope behavior; remove this wrapper. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#Service:
  replacement: "Context.Service"
  note: "Replace the `effect` constructor option with `make`. V4 does not generate a `Default` layer or wire `dependencies`; define a `Layer.effect` and provide its dependencies explicitly."
effect/Effect#Service.AllowedType:
  replacement: "Context.Service"
  note: "Service type machinery moved to `Context.Service`; do not reference its internal helper types. Adapt arguments and imports to the v4 API."
effect/Effect#Service.Class:
  replacement: "Context.Service"
  note: "Service classes are now defined with `Context.Service`. Adapt arguments and imports to the v4 API."
effect/Effect#Service.HasArguments:
  replacement: "Context.Service"
  note: "Service constructor typing is handled by `Context.Service`. Adapt arguments and imports to the v4 API."
effect/Effect#Service.MakeAccessors:
  replacement: "Context.Service"
  note: "Use the generated `.use` helper instead of v3 accessor type machinery. Adapt arguments and imports to the v4 API."
effect/Effect#Service.MakeArguments:
  replacement: "Context.Service"
  note: "Pass a `make` Effect in the v4 `Context.Service` options. Adapt arguments and imports to the v4 API."
effect/Effect#Service.MakeContext:
  replacement: "Context.Service"
  note: "Service context typing is inferred by `Context.Service`. Adapt arguments and imports to the v4 API."
effect/Effect#Service.MakeDeps:
  replacement: "Layer.provide"
  note: "Compose service dependencies explicitly with Layers. Adapt arguments and imports to the v4 API."
effect/Effect#Service.MakeDepsE:
  replacement: "Layer.Error"
  note: "Use Layer channel extractors rather than Service internals. Adapt arguments and imports to the v4 API."
effect/Effect#Service.MakeDepsIn:
  replacement: "Layer.Services"
  note: "Use Layer channel extractors rather than Service internals. Adapt arguments and imports to the v4 API."
effect/Effect#Service.MakeDepsOut:
  replacement: "Layer.Success"
  note: "Use Layer channel extractors rather than Service internals. Adapt arguments and imports to the v4 API."
effect/Effect#Service.MakeError:
  replacement: "Layer.Error"
  note: "Use the Layer error-channel extractor. Adapt arguments and imports to the v4 API."
effect/Effect#Service.MakeService:
  replacement: "Context.Service"
  note: "The service shape is inferred by `Context.Service`. Adapt arguments and imports to the v4 API."
effect/Effect#Service.ProhibitedType:
  replacement: "Context.Service"
  note: "Do not reference the removed internal validation type. Adapt arguments and imports to the v4 API."
effect/Effect#serviceConstants:
  replacement: "Context.Service.use"
  note: "Expose constants from the service explicitly or through the generated `use` helper. Adapt arguments and imports to the v4 API."
effect/Effect#serviceFunction:
  replacement: "Context.Service.use"
  note: "Use the service class `.use` helper to build an accessor function. Adapt arguments and imports to the v4 API."
effect/Effect#serviceFunctionEffect:
  replacement: "Context.Service.use"
  note: "Use the service class `.use` helper for effect-returning methods. Adapt arguments and imports to the v4 API."
effect/Effect#serviceFunctions:
  replacement: "Context.Service.use"
  note: "Define explicit service accessors with the generated `.use` helper. Adapt arguments and imports to the v4 API."
effect/Effect#serviceMembers:
  replacement: "Context.Service.use"
  note: "Define explicit service accessors with the generated `.use` helper. Adapt arguments and imports to the v4 API."
effect/Effect#serviceOptional:
  replacement: "service"
  note: "Services are Effects in v4; yield or compose the service key directly. Use `Effect.serviceOption` only when absence is expected."
effect/Effect#setFiberRefs:
  replacement: "none"
  note: "Bulk FiberRefs replacement was removed; provide individual context references. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#step:
  replacement: "none"
  note: "The low-level Effect stepping API was removed from the public surface. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#summarized:
  replacement: "Effect.gen"
  note: "Run the summary Effect before and after the target Effect and combine the two measurements explicitly. Adapt arguments and imports to the v4 API."
effect/Effect#supervised:
  replacement: "FiberSet"
  note: "Track explicitly forked Fibers in a scoped `FiberSet` instead of installing a runtime Supervisor. Adapt arguments and imports to the v4 API."
effect/Effect#Tag:
  replacement: "Context.Service"
  note: "Define services with `Context.Service`; use the generated `.use` helper for accessors. Adapt arguments and imports to the v4 API."
effect/Effect#Tag.AllowedType:
  replacement: "Context.Service"
  note: "Tag validation internals were removed; use `Context.Service` directly. Adapt arguments and imports to the v4 API."
effect/Effect#Tag.ProhibitedType:
  replacement: "Context.Service"
  note: "Tag validation internals were removed; use `Context.Service` directly. Adapt arguments and imports to the v4 API."
effect/Effect#Tag.Proxy:
  replacement: "Context.Service.use"
  note: "Replace proxy accessors with the generated `.use` helper. Adapt arguments and imports to the v4 API."
effect/Effect#tagMetrics:
  replacement: "Metric.withAttributes"
  note: "Convert key/value tags to metric attributes. Adapt arguments and imports to the v4 API."
effect/Effect#tagMetricsScoped:
  replacement: "Metric.withAttributes"
  note: "Apply attributes around the scoped Effect. Adapt arguments and imports to the v4 API."
effect/Effect#takeUntil:
  replacement: "none"
  note: "Use an explicit `Effect.gen` loop for an effectful stopping predicate. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#takeWhile:
  replacement: "none"
  note: "Use an explicit `Effect.gen` loop, or `Array.takeWhile` when the predicate is pure. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#tapBoth:
  replacement: "Effect.tapError + Effect.tap"
  note: "Tap the failure path first, then tap successful values. Adapt arguments and imports to the v4 API."
effect/Effect#tapErrorCause:
  replacement: "Effect.tapCause"
  note: "Use the shortened v4 cause-tap name. Adapt arguments and imports to the v4 API."
effect/Effect#timedWith:
  replacement: "Effect.gen"
  note: "Read the supplied clock Effect before and after the target and compute the Duration explicitly. Adapt arguments and imports to the v4 API."
effect/Effect#timeoutFail:
  replacement: "Effect.timeoutOrElse"
  note: "Use `Effect.fail(onTimeout())` as the timeout fallback. Adapt arguments and imports to the v4 API."
effect/Effect#timeoutFailCause:
  replacement: "Effect.timeoutOrElse"
  note: "Use `Effect.failCause(onTimeout())` as the timeout fallback. Adapt arguments and imports to the v4 API."
effect/Effect#timeoutTo:
  replacement: "Effect.timeoutOrElse + Effect.map"
  note: "Map successful values first and use the timeout fallback for `onTimeout`. Adapt arguments and imports to the v4 API."
effect/Effect#tracerWith:
  replacement: "Tracer.Tracer.use"
  note: "Use the Tracer reference's `.use` helper to invoke the effectful callback. Adapt arguments and imports to the v4 API."
effect/Effect#transplant:
  replacement: "none"
  note: "Fiber scope grafting was removed; use structured concurrency with `forkChild`, `forkScoped`, or `forkIn`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#transposeMapOption:
  replacement: "Option.match"
  note: "Return `Effect.succeedNone` for None and map the Effect result to Some. Adapt arguments and imports to the v4 API."
effect/Effect#try:
  replacement: "Effect.try"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#tryMap:
  replacement: "Effect.flatMap + Effect.try"
  note: "FlatMap the source value into the v4 synchronous try constructor. Adapt arguments and imports to the v4 API."
effect/Effect#tryMapPromise:
  replacement: "Effect.flatMap + Effect.tryPromise"
  note: "FlatMap the source value into the v4 Promise try constructor. Adapt arguments and imports to the v4 API."
effect/Effect#unless:
  replacement: "Effect.suspend"
  note: "Select `Effect.void` or the target Effect with a negated lazy condition. Adapt arguments and imports to the v4 API."
effect/Effect#unlessEffect:
  replacement: "Effect.when"
  note: "Negate the effectful boolean condition, then use the consolidated `when`. Adapt arguments and imports to the v4 API."
effect/Effect#unsafeMakeLatch:
  replacement: "Latch.makeUnsafe"
  note: "The unsafe constructor moved to `effect/Latch`. Adapt arguments and imports to the v4 API."
effect/Effect#unsafeMakeSemaphore:
  replacement: "Semaphore.makeUnsafe"
  note: "The unsafe constructor moved to `effect/Semaphore`. Adapt arguments and imports to the v4 API."
effect/Effect#unsandbox:
  replacement: "Effect.catch + Effect.failCause"
  note: "Treat the sandboxed Cause as an error and fail with that Cause. Adapt arguments and imports to the v4 API."
effect/Effect#updateFiberRefs:
  replacement: "none"
  note: "Bulk FiberRefs updates were removed; update individual context references with `Effect.updateService`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#using:
  replacement: "Effect.scoped + Effect.flatMap"
  note: "Acquire inside a fresh Scope, run the use Effect, and close the Scope afterward. Adapt arguments and imports to the v4 API."
effect/Effect#validateAll:
  replacement: "Effect.validate"
  note: "Use the consolidated collection validation combinator. Adapt arguments and imports to the v4 API."
effect/Effect#validateFirst:
  replacement: "Effect.firstSuccessOf"
  note: "Map inputs to validation effects and select the first success; handle accumulated diagnostics explicitly if required. Adapt arguments and imports to the v4 API."
effect/Effect#validateWith:
  replacement: "Effect.zipWith"
  note: "Zip and combine the Effects; use `mode: \"result\"` when both failures must be retained. Adapt arguments and imports to the v4 API."
effect/Effect#whenEffect:
  replacement: "Effect.when"
  note: "The v4 `when` combinator accepts an effectful boolean condition directly. Adapt arguments and imports to the v4 API."
effect/Effect#whenFiberRef:
  replacement: "reference.use + Effect.when"
  note: "Use the Context.Reference `.use` helper to inspect the value, test it, and branch explicitly. Adapt arguments and imports to the v4 API."
effect/Effect#whenLogLevel:
  replacement: "none"
  note: "Log-level conditional execution was removed; configure Logger filtering and guard optional work explicitly. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#whenRef:
  replacement: "Ref.get + Effect.flatMap"
  note: "Read the Ref, test it, and branch explicitly. Adapt arguments and imports to the v4 API."
effect/Effect#withClock:
  replacement: "Effect.provideService"
  note: "Provide `Clock.Clock` for the target Effect. Adapt arguments and imports to the v4 API."
effect/Effect#withClockScoped:
  replacement: "Effect.provideService"
  note: "Provide `Clock.Clock` around the scoped Effect. Adapt arguments and imports to the v4 API."
effect/Effect#withConcurrency:
  replacement: "none"
  note: "Ambient concurrency was removed; pass `concurrency` directly to `Effect.all`, `Effect.forEach`, and related combinators. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withConfigProvider:
  replacement: "Effect.provideService"
  note: "Provide `ConfigProvider.ConfigProvider` for the target Effect. Adapt arguments and imports to the v4 API."
effect/Effect#withConfigProviderScoped:
  replacement: "Effect.provideService"
  note: "Provide the ConfigProvider around the scoped Effect. Adapt arguments and imports to the v4 API."
effect/Effect#withConsole:
  replacement: "Effect.provideService"
  note: "Provide `Console.Console` for the target Effect. Adapt arguments and imports to the v4 API."
effect/Effect#withConsoleScoped:
  replacement: "Effect.provideService"
  note: "Provide the Console service around the scoped Effect. Adapt arguments and imports to the v4 API."
effect/Effect#withEarlyRelease:
  replacement: "Scope.make + Scope.close"
  note: "Create a Scope explicitly, provide it to acquisition, and retain a close action. Adapt arguments and imports to the v4 API."
effect/Effect#withFiberRuntime:
  replacement: "none"
  note: "Direct FiberRuntime access was removed; use public Effect, Fiber, and Context operations. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withLogSpan:
  replacement: "Effect.withLogSpan"
  note: "Still exported in v4; update call sites for the revised signature, options, and channel inference."
effect/Effect#withMaxOpsBeforeYield:
  replacement: "none"
  note: "The scheduler operation budget is no longer configurable through Effect. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withMetric:
  replacement: "Effect.tap + Metric.update"
  note: "Update the Metric explicitly from the Effect success value. Adapt arguments and imports to the v4 API."
effect/Effect#withRandom:
  replacement: "Effect.provideService"
  note: "Provide `Random.Random` for the target Effect. Adapt arguments and imports to the v4 API."
effect/Effect#withRandomFixed:
  replacement: "Effect.provideService"
  note: "Provide a custom deterministic `Random.Random` implementation. Adapt arguments and imports to the v4 API."
effect/Effect#withRandomScoped:
  replacement: "Effect.provideService"
  note: "Provide the Random service around the scoped Effect. Adapt arguments and imports to the v4 API."
effect/Effect#withRequestBatching:
  replacement: "none"
  note: "Ambient request batching configuration was removed; configure batching in the `RequestResolver`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withRequestCache:
  replacement: "none"
  note: "Ambient request-cache replacement was removed; model keyed caching explicitly with `Cache`. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withRequestCaching:
  replacement: "none"
  note: "Ambient request caching was removed; configure resolution or use `Cache` explicitly. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withRuntimeFlagsPatch:
  replacement: "none"
  note: "RuntimeFlags patching was removed from the public API. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withRuntimeFlagsPatchScoped:
  replacement: "none"
  note: "Scoped RuntimeFlags patching was removed from the public API. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withScheduler:
  replacement: "none"
  note: "Ambient scheduler replacement was removed; use supported runtime run options or explicit scheduling combinators. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withSchedulingPriority:
  replacement: "none"
  note: "Ambient fiber scheduling priority was removed from the public API. No direct public replacement exists in v4; rewrite the call site around the stated v4 primitive."
effect/Effect#withTracerScoped:
  replacement: "Effect.provideService"
  note: "Provide the Tracer service around the scoped Effect. Adapt arguments and imports to the v4 API."
effect/Effect#withUnhandledErrorLogLevel:
  replacement: "Effect.ignore"
  note: "Handle or explicitly ignore child-fiber failures, selecting the desired log behavior at the boundary. Adapt arguments and imports to the v4 API."
effect/Effect#zipLeft:
  replacement: "Effect.zip + Effect.map"
  note: "Zip the Effects and select the first tuple element. Adapt arguments and imports to the v4 API."
effect/Effect#zipRight:
  replacement: "Effect.andThen"
  note: "Sequence the Effects and retain the second result. Adapt arguments and imports to the v4 API."
