"effect/List#append":
  replacement: "Array.append"
  note: "List was removed; use Array.append. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#appendAll":
  replacement: "Array.appendAll"
  note: "List was removed; use Array.appendAll. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#cons":
  replacement: "Array.prepend"
  note: "List was removed; change List.cons(head, tail) to Array.prepend(tail, head)."
"effect/List#Cons":
  replacement: "Array.NonEmptyReadonlyArray"
  note: "Use the immutable non-empty array type; constructors may return the assignable mutable NonEmptyArray subtype."
"effect/List#empty":
  replacement: "Array.empty"
  note: "List was removed; use Array.empty. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#filter":
  replacement: "Array.filter"
  note: "List was removed; use Array.filter. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#filterMap":
  replacement: "Array.filterMap"
  note: "List was removed; use Array.filterMap and change the callback from Option to Result."
"effect/List#fromIterable":
  replacement: "Array.fromIterable"
  note: "List was removed; use Array.fromIterable. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#getEquivalence":
  replacement: "Array.makeEquivalence"
  note: "List was removed; compare the replacement arrays with Array.makeEquivalence."
"effect/List#head":
  replacement: "Array.head"
  note: "List was removed; use Array.head. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#isCons":
  replacement: "Array.isReadonlyArrayNonEmpty"
  note: "List was removed; this checks that the replacement readonly array is non-empty."
"effect/List#isList":
  replacement: "Array.isArray"
  note: "The List brand is gone; this now recognizes the replacement JavaScript array representation."
"effect/List#isNil":
  replacement: "Array.isReadonlyArrayEmpty"
  note: "List was removed; this checks that the replacement readonly array is empty."
"effect/List#last":
  replacement: "Array.last"
  note: "List was removed; use Array.last. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#List":
  replacement: "ReadonlyArray"
  note: "Replace the persistent linked-list representation with ReadonlyArray<A>."
"effect/List#List.AndNonEmpty":
  replacement: "Array.ReadonlyArray.AndNonEmpty"
  note: "Use the corresponding readonly-array utility type."
"effect/List#List.OrNonEmpty":
  replacement: "Array.ReadonlyArray.OrNonEmpty"
  note: "Use the corresponding readonly-array utility type."
"effect/List#List.With":
  replacement: "Array.ReadonlyArray.With"
  note: "Use the corresponding readonly-array utility type."
"effect/List#make":
  replacement: "Array.make"
  note: "List was removed; use Array.make. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#map":
  replacement: "Array.map"
  note: "List was removed; use Array.map. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#nil":
  replacement: "Array.empty"
  note: "List was removed; represent Nil with an empty array."
"effect/List#Nil":
  replacement: "none"
  note: "Represent this case as readonly []; there is no tagged Nil interface in v4."
"effect/List#of":
  replacement: "Array.of"
  note: "List was removed; use Array.of. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#partition":
  replacement: "Array.partition"
  note: "Use a Result-returning callback: failure values form the first array and success values the second."
"effect/List#partitionMap":
  replacement: "Array.partition"
  note: "Migrate the Either-returning mapper to Result; failures form the first array and successes the second."
"effect/List#prependAll":
  replacement: "Array.prependAll"
  note: "List was removed; use Array.prependAll. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#prependAllReversed":
  replacement: "Array.prependAll + Array.reverse"
  note: "Use Array.prependAll(self, Array.reverse(prefix)) to preserve the old ordering."
"effect/List#reduce":
  replacement: "Array.reduce"
  note: "List was removed; use Array.reduce. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#reduceRight":
  replacement: "Array.reduceRight"
  note: "List was removed; use Array.reduceRight. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#reverse":
  replacement: "Array.reverse"
  note: "List was removed; use Array.reverse. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#size":
  replacement: "Array.length"
  note: "List was removed; use the replacement array length helper or the .length property."
"effect/List#splitAt":
  replacement: "Array.splitAt"
  note: "List was removed; use Array.splitAt. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#tail":
  replacement: "Array.tail"
  note: "List was removed; use Array.tail. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#take":
  replacement: "Array.take"
  note: "List was removed; use Array.take. It preserves ordering but returns arrays rather than persistent linked lists."
"effect/List#toArray":
  replacement: "Array.fromIterable"
  note: "After migrating the representation this is usually unnecessary; use Array.fromIterable when a fresh mutable array is required."
"effect/List#toChunk":
  replacement: "Chunk.fromIterable"
  note: "Convert the replacement array or other iterable with Chunk.fromIterable."
"effect/List#TypeId":
  replacement: "none"
  note: "Arrays have no List runtime marker; remove TypeId inspection."
"effect/List#unsafeHead":
  replacement: "Array.headNonEmpty"
  note: "Use a NonEmptyReadonlyArray proof before accessing the head; the v4 helper does not accept an empty array."
"effect/List#unsafeLast":
  replacement: "Array.lastNonEmpty"
  note: "Use a NonEmptyReadonlyArray proof before accessing the last element; the v4 helper does not accept an empty array."
"effect/List#unsafeTail":
  replacement: "Array.tailNonEmpty"
  note: "Use a NonEmptyReadonlyArray proof before taking the tail; the v4 helper does not accept an empty array."
"effect/List#every":
  replacement: "Array.every"
  note: "List was removed; run the predicate against the replacement array with Array.every."
"effect/List#some":
  replacement: "Array.some"
  note: "List was removed; run the predicate against the replacement array with Array.some."
