"effect/SingleProducerAsyncInput#AsyncInputConsumer":
  replacement: "Queue.Dequeue"
  note: "Use v4 Queue producer and consumer views and bridge to Channel with Channel.fromQueue, Channel.callback, or Channel.fromPull. Queue.make({ capacity: 0 }) is the closest rendezvous backpressure but is not one-for-one."
"effect/SingleProducerAsyncInput#AsyncInputProducer":
  replacement: "Queue.Enqueue"
  note: "Use v4 Queue producer and consumer views and bridge to Channel with Channel.fromQueue, Channel.callback, or Channel.fromPull. Queue.make({ capacity: 0 }) is the closest rendezvous backpressure but is not one-for-one."
"effect/SingleProducerAsyncInput#make":
  replacement: "Queue.make"
  note: "Use v4 Queue producer and consumer views and bridge to Channel with Channel.fromQueue, Channel.callback, or Channel.fromPull. Queue.make({ capacity: 0 }) is the closest rendezvous backpressure but is not one-for-one."
"effect/SingleProducerAsyncInput#SingleProducerAsyncInput":
  replacement: "Queue.Queue"
  note: "Use v4 Queue producer and consumer views and bridge to Channel with Channel.fromQueue, Channel.callback, or Channel.fromPull. Queue.make({ capacity: 0 }) is the closest rendezvous backpressure but is not one-for-one."
