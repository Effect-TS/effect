effect/RuntimeFlagsPatch#both:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; combine the replacement semantic configurations directly."
effect/RuntimeFlagsPatch#disable:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; disable the corresponding semantic behavior directly."
effect/RuntimeFlagsPatch#disabledSet:
  replacement: "none"
  note: "RuntimeFlagsPatch and its disabled-bit set were removed."
effect/RuntimeFlagsPatch#either:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; combine the replacement semantic configurations directly."
effect/RuntimeFlagsPatch#empty:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; no aggregate patch value is needed."
effect/RuntimeFlagsPatch#enable:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; enable the corresponding semantic behavior directly."
effect/RuntimeFlagsPatch#enabledSet:
  replacement: "none"
  note: "RuntimeFlagsPatch and its enabled-bit set were removed."
effect/RuntimeFlagsPatch#exclude:
  replacement: "none"
  note: "RuntimeFlagsPatch and aggregate patch set operations were removed."
effect/RuntimeFlagsPatch#includes:
  replacement: "none"
  note: "RuntimeFlagsPatch and aggregate patch queries were removed."
effect/RuntimeFlagsPatch#inverse:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; configure inverse semantic behavior explicitly."
effect/RuntimeFlagsPatch#isActive:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; inspect the corresponding semantic facility if needed."
effect/RuntimeFlagsPatch#isDisabled:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; inspect the corresponding semantic facility if needed."
effect/RuntimeFlagsPatch#isEmpty:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; no aggregate patch value remains to query."
effect/RuntimeFlagsPatch#make:
  replacement: "none"
  note: "RuntimeFlagsPatch was removed; configure scheduler yielding, interruptibility, or metrics directly."
effect/RuntimeFlagsPatch#render:
  replacement: "none"
  note: "RuntimeFlagsPatch and its renderer were removed."
effect/RuntimeFlagsPatch#RuntimeFlagsPatch:
  replacement: "none"
  note: "The aggregate runtime-flags patch abstraction was removed."
