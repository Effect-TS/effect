"@effect/platform/HttpMultiplex#add":
  replacement: "none"
  note: "No v4 multiplex abstraction exists; add a predicate and app to a custom first-match dispatcher."
"@effect/platform/HttpMultiplex#empty":
  replacement: "none"
  note: "No v4 multiplex value exists; initialize an empty custom dispatcher."
"@effect/platform/HttpMultiplex#headerEndsWith":
  replacement: "none"
  note: "Use a custom dispatcher predicate over the lower-cased request header and String.endsWith."
"@effect/platform/HttpMultiplex#headerExact":
  replacement: "none"
  note: "Use a custom dispatcher predicate over the lower-cased request header with exact equality."
"@effect/platform/HttpMultiplex#headerRegex":
  replacement: "none"
  note: "Use a custom dispatcher predicate over the lower-cased request header and RegExp.test."
"@effect/platform/HttpMultiplex#headerStartsWith":
  replacement: "none"
  note: "Use a custom dispatcher predicate over the lower-cased request header and String.startsWith."
"@effect/platform/HttpMultiplex#hostEndsWith":
  replacement: "none"
  note: "Use a custom dispatcher predicate over request.headers.host and String.endsWith."
"@effect/platform/HttpMultiplex#hostExact":
  replacement: "none"
  note: "Use a custom dispatcher predicate comparing request.headers.host exactly."
"@effect/platform/HttpMultiplex#hostRegex":
  replacement: "none"
  note: "Use a custom dispatcher predicate over request.headers.host and RegExp.test."
"@effect/platform/HttpMultiplex#hostStartsWith":
  replacement: "none"
  note: "Use a custom dispatcher predicate over request.headers.host and String.startsWith."
"@effect/platform/HttpMultiplex#HttpMultiplex":
  replacement: "none"
  note: "Represent the removed model as a first-match Effect dispatcher requiring HttpServerRequest."
"@effect/platform/HttpMultiplex#make":
  replacement: "none"
  note: "No constructor was ported; fold predicate/app pairs into a first-match Effect dispatcher."
"@effect/platform/HttpMultiplex#TypeId":
  replacement: "none"
  note: "The module and nominal type id were removed with no v4 counterpart."
