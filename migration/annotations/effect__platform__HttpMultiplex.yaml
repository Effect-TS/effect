"@effect/platform/HttpMultiplex":
  replacement: none
  note: No v4 multiplex abstraction exists; add a predicate and app to a custom first-match dispatcher. No v4 multiplex value exists; initialize an empty custom dispatcher. Use a custom dispatcher predicate over the lower-cased request header and String.endsWith. Use a custom dispatcher predicate over the lower-cased request header with exact equality. Use a custom dispatcher predicate over the lower-cased request header and RegExp.test. Use a custom dispatcher predicate over the lower-cased request header and String.startsWith. Use a custom dispatcher predicate over request.headers.host and String.endsWith. Use a custom dispatcher predicate comparing request.headers.host exactly. Use a custom dispatcher predicate over request.headers.host and RegExp.test. Use a custom dispatcher predicate over request.headers.host and String.startsWith. Represent the removed model as a first-match Effect dispatcher requiring HttpServerRequest. No constructor was ported; fold predicate/app pairs into a first-match Effect dispatcher. The module and nominal type id were removed with no v4 counterpart.
