effect/TArray#TArray:
  replacement: "TxChunk.TxChunk"
  note: "TArray has no direct v4 counterpart; TxChunk is the closest rewrite target but uses whole-Chunk operations."
effect/TArray#TArray.Variance:
  replacement: "none"
  note: "TArray was removed and TxChunk exposes no public variance marker."
effect/TArray#TArrayTypeId:
  replacement: "TxChunk.isTxChunk"
  note: "TArray and its public type id were removed; use the TxChunk runtime guard after rewriting the data structure."
effect/TArray#collectFirst:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#collectFirstSTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#contains:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#count:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#countSTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#empty:
  replacement: "TxChunk.empty"
  note: "TArray was removed; TxChunk is the closest v4 transactional indexed collection."
effect/TArray#everySTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#findFirstIndex:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#findFirstIndexFrom:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#findFirstIndexWhere:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#findFirstIndexWhereFrom:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#findFirstIndexWhereFromSTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#findFirstIndexWhereSTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#findFirstSTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#findLast:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#findLastIndex:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#findLastIndexFrom:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#findLastSTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#fromIterable:
  replacement: "TxChunk.fromIterable"
  note: "TArray was removed; construct the v4 TxChunk rewrite target from the iterable."
effect/TArray#get:
  replacement: "Effect.map(TxChunk.get(self), Chunk.get(index))"
  note: "TxChunk.get returns the whole Chunk, so apply Chunk.get to preserve indexed optional lookup."
effect/TArray#headOption:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#lastOption:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#make:
  replacement: "TxChunk.fromIterable(elements)"
  note: "TxChunk.make takes one Chunk rather than variadic elements; TxChunk.fromIterable preserves the old call shape after collecting arguments."
effect/TArray#maxOption:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#minOption:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#reduce:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#reduceOption:
  replacement: "TxChunk.get + Chunk/Array operation"
  note: "TArray was removed. Read the TxChunk snapshot and perform the equivalent pure collection query inside the surrounding Effect.tx transaction."
effect/TArray#reduceOptionSTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#reduceSTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#size:
  replacement: "TxChunk.size"
  note: "TxChunk is the closest v4 rewrite target. V4 Tx operations return ordinary Effects; compose multiple operations under one outer Effect.tx to keep them atomic."
effect/TArray#someSTM:
  replacement: "Effect.tx + TxChunk.get + Effect traversal"
  note: "TArray was removed. Read the TxChunk snapshot and perform the effectful traversal explicitly within the same Effect.tx transaction."
effect/TArray#toArray:
  replacement: "Effect.map(TxChunk.get(self), Chunk.toArray)"
  note: "TxChunk.get returns a Chunk; convert that snapshot to an Array explicitly."
effect/TArray#transform:
  replacement: "TxChunk.update(self, Chunk.map(f))"
  note: "TArray was removed; transform the whole TxChunk snapshot with a Chunk mapping function."
effect/TArray#transformSTM:
  replacement: "Effect.tx + TxChunk.get/TxChunk.set"
  note: "Read the snapshot, traverse it effectfully, and write the rebuilt Chunk within one Effect.tx transaction."
effect/TArray#update:
  replacement: "TxChunk.modify"
  note: "TxChunk updates the whole Chunk; use modify to update the indexed element and preserve the old optional-index behavior."
effect/TArray#updateSTM:
  replacement: "Effect.tx + TxChunk.get/TxChunk.set"
  note: "Read, effectfully update the indexed element, and write the rebuilt Chunk within one Effect.tx transaction."
effect/TArray#every:
  replacement: "Effect.map(TxChunk.get(self), Chunk.every(predicate))"
  note: "TArray was removed; read the TxChunk snapshot and test every element inside the surrounding Effect.tx transaction."
effect/TArray#some:
  replacement: "Effect.map(TxChunk.get(self), Chunk.some(predicate))"
  note: "TArray was removed; read the TxChunk snapshot and test for a matching element inside the surrounding Effect.tx transaction."
